# MASK MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the first compiling MVP of `mask` as a panic-free, async-first LAN-discovered RAG service for shared code knowledge retrieval.

**Architecture:** The binary exposes two subcommands: `serve` builds an in-memory embedding index from a directory, announces itself through CAMP, and serves `POST /query` over Axum; `query` discovers a `knowledge-base` peer through CAMP, forwards the question with Reqwest, and prints only the JSON result array. The code stays split by responsibility so indexing, serving, output formatting, and CLI orchestration can evolve independently.

**Tech Stack:** Rust 2024, tokio, clap, axum, reqwest, serde, serde_json, fastembed, coding_agent_mesh_presence.

---

### Task 1: Replace the old WASP entrypoints with MASK module boundaries

**Files:**
- Modify: `src/main.rs`
- Modify: `src/cli.rs`
- Create: `src/indexer.rs`
- Create: `src/server.rs`
- Modify: `src/output.rs`

- [ ] Step 1: Define the new module tree and async entrypoint.
- [ ] Step 2: Replace the old `run` CLI with `serve` and `query` subcommands.
- [ ] Step 3: Add shared request/response data types in `output.rs`.
- [ ] Step 4: Wire command dispatch in `main.rs`.

### Task 2: Lock the JSON output contract with failing tests

**Files:**
- Create: `tests/query_output_contract.rs`
- Create: `tests/cli_contract.rs`

- [ ] Step 1: Write a test that serializes query results to a strict JSON array.
- [ ] Step 2: Run the targeted test and observe failure.
- [ ] Step 3: Implement the minimal output helpers to pass.
- [ ] Step 4: Run the targeted tests and keep them green.

### Task 3: Build the indexer with test-first chunking and ranking

**Files:**
- Create: `src/indexer.rs`
- Create: `tests/indexer_contract.rs`

- [ ] Step 1: Write a failing test for recursive file discovery + naive chunking.
- [ ] Step 2: Write a failing test for cosine ranking over deterministic vectors.
- [ ] Step 3: Implement panic-free text filtering, chunk generation, and similarity helpers.
- [ ] Step 4: Add the embedding-model integration boundary so `fastembed` can plug in behind tested helpers.
- [ ] Step 5: Re-run the targeted test suite.

### Task 4: Build the server and CAMP discovery flow

**Files:**
- Create: `src/server.rs`
- Create: `tests/server_contract.rs`

- [ ] Step 1: Write a failing test for `POST /query` using in-memory state.
- [ ] Step 2: Write a failing test for discovery target resolution from CAMP agent info.
- [ ] Step 3: Implement the Axum router, query handler, and ZeroConfMesh announcement/discovery helpers.
- [ ] Step 4: Re-run the targeted tests.

### Task 5: Integrate end-to-end CLI behavior

**Files:**
- Modify: `src/main.rs`
- Modify: `src/cli.rs`
- Modify: `src/server.rs`
- Modify: `src/output.rs`
- Create: `tests/e2e_contract.rs`

- [ ] Step 1: Write failing tests for `mask query` stdout purity and discovered HTTP request behavior.
- [ ] Step 2: Implement the minimal client flow with Reqwest and stdout-only JSON emission.
- [ ] Step 3: Run all focused tests.
- [ ] Step 4: Refactor only if tests stay green.

### Task 6: Verify the crate with formatting, linting, and tests

**Files:**
- Modify: `Cargo.toml` (only if verification exposes dependency/lint issues)

- [ ] Step 1: Run `cargo fmt --all`.
- [ ] Step 2: Run `cargo clippy --all-targets --all-features -- -D warnings`.
- [ ] Step 3: Run `cargo test`.
- [ ] Step 4: Record any remaining risks if dependency fetch or sibling-crate issues block full verification.
